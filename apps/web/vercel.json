{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "corepack enable && pnpm -w install --frozen-lockfile",
  "buildCommand": "pnpm -w --filter ./apps/web build"
}

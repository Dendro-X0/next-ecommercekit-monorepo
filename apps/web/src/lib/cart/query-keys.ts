/**
 * Cart query keys.
 * One export per file: `CART_QK`.
 */

/**
 * Query key identifying the current user's cart (cookie-based).
 */
export const CART_QK: readonly ["shop", "cart"] = ["shop", "cart"] as const
